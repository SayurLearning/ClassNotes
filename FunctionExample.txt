Previous hw
Create a list of food items for a cafe. Create another list with price of those items.
 Create another list with the supply of those items (how many items are available in the cafe) in the starting of the day
  Calculate the sales for the cafe. Cafe can have a maximum of 10 transactions.

Extension of the hw to introduce function
    Replenish supply for every 5 customers (done within for loop) and at the end of theday (outside of for loop)
Extension of the hw to introduce functions with parameter
    Calculate replenish supply at the starting of the day for hot items, every 5 customers for all items and only cold items 
    at the every end of the day.

 
 Another example 
 Previous Hw
  In the input, find the first A and last A, print all the letters between these two A.

 Extension of the hw to introduce function with parameter
  In the input, find the first A and last A, print all the letters between these two A. 
  If there is no A or 2nd A is not found, find the first B  and last B and print all the letters between these two B. 
  If there is no B or 2nd B is not found, find the first C and last C and print all the letters between these two C. 



